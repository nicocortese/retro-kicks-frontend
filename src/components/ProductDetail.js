"use client";
import React from 'react';

const ProductDetail = ({ id }) => {
  // Por ahora, solo mostramos un texto simple
  return (
    <div>
      <h1>Página de Detalle de Producto</h1>
      <p>Probando el componente.</p>
    </div>
  );
};

export default ProductDetail;